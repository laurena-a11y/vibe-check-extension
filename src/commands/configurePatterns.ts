import * as vscode from 'vscode';

export async function configurePatterns(context: vscode.ExtensionContext): Promise<void> {
    vscode.window.showInformationMessage('Configure Pattern Libraries - Coming soon!');

    // TODO: Open webview with configuration UI
    // TODO: Allow users to add Figma URLs or web URLs
    // TODO: Save configuration to workspace/global settings
}
